language: python
python:
  - "3.6"
  - "3.5"

# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install -r test_requirements.txt
  - pip install .
# command to run tests
script: pytest --cov=json_justify/
env:
  - CODECOV_TOKEN="a4163717-3c5f-4519-a231-4684075d1ba9"
after_success:
  - coveralls
